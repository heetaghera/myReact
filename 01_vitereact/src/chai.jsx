function Chai(){
    return (
        <h1>chai is ready</h1>
    )
}

export default Chai

// 1)name of component should start from capital letter like 'Chai' not 'chai' 
// (file name can start with either capital or small letter but name of component(function) should be capital)
// 2)file extension should be '.jsx' but not '.js'